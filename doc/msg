cmd sent need get reply:
init
sent
received, put on queue
ok or error after executed
committed, sync to disk

all msg should end with 'error' or 'committed'


两个节点间消息的传输在一段时间内维持sn的递增，即使一端重新建立连接
    
    connect

    sn = 0
                             sn = 0
                             ack = 1
    sn = 1
    ack =1
    
首先必须有一个可靠的消息传递系统
消息是否允许乱序到达？

是否可以依托于tcp本身的连接，还是需要在tcp上再建一层？

MessageChannel:


NetworkIO:
    
    OpenSession()
    
    
    Call(function, args, block, timeout)
    整个操作在timeout之后会取消，放弃执行，不管是同步还是异步IO
    如果block为True的时候，需要指定timeout



    def 
    wait4_send_queue
    wait4_ack_queue
    wait4_commit_queue



    Write()
    Read()
    

    Close()
    

class IO:
    
    

    


